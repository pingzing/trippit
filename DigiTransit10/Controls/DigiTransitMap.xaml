<UserControl
    x:Class="DigiTransit10.Controls.DigiTransitMap"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DigiTransit10.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <!--MapElements (contains MapIcons and MapPolylines) cannot be databound; it must have stuff added in code-behind.
        XAML stuff can live in the MapItemsControl, which _can_ be databound.-->
        <Maps:MapControl x:Name="DigiTransitMapControl">
        </Maps:MapControl>

    </Grid>
</UserControl>
