<UserControl
    x:Class="DigiTransit10.Controls.TripResultContent"
    x:Name="ThisTripResultContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DigiTransit10.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tripPlanStrip="using:DigiTransit10.Controls.TripPlanStrip"
    xmlns:Behaviors="using:Template10.Behaviors"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Media="using:Microsoft.Xaml.Interactions.Media"
    xmlns:models="using:DigiTransit10.Models"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    xmlns:apiModels="using:DigiTransit10.Models.ApiModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>

    </UserControl.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="TripStateGroup">
                <VisualStateGroup.Transitions>
                    <VisualTransition From="TripsList" GeneratedDuration="0" To="DetailsList">
                        <Storyboard />
                    </VisualTransition>
                    <VisualTransition From="DetailsList" GeneratedDuration="0" To="TripsList" />
                </VisualStateGroup.Transitions>
                <VisualState x:Name="TripsList" />
                <VisualState x:Name="DetailsList">
                    <VisualState.Setters>
                        <Setter Target="DirectionsFloatingPanel.(UIElement.Visibility)" Value="Visible" />
                        <Setter Target="TripResultList.(UIElement.Visibility)" Value="Collapsed" />
                        <Setter Target="SingleMap.(UIElement.Visibility)" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            
            <VisualStateGroup x:Name="FloatingPanelStateGroup">
                <VisualStateGroup.Transitions>
                    <VisualTransition From="PanelExpanded" GeneratedDuration="0:0:0.5" To="PanelCollapsed">
                        <Storyboard>
                            <DoubleAnimation x:Name="ExpandedTranslationAnimation" Duration="0:0:0.5" To="300" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)" Storyboard.TargetName="DirectionsFloatingPanel">
                                <DoubleAnimation.EasingFunction>
                                    <QuarticEase EasingMode="EaseInOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </VisualTransition>
                    <VisualTransition From="PanelCollapsed" GeneratedDuration="0:0:0.5" To="PanelExpanded">
                        <Storyboard>
                            <DoubleAnimationUsingKeyFrames EnableDependentAnimation="True" Storyboard.TargetProperty="(FrameworkElement.Height)" Storyboard.TargetName="DirectionsFloatingPanel">
                                <EasingDoubleKeyFrame x:Name="PanelExpandHeightValue" KeyTime="0" Value="400"/>
                            </DoubleAnimationUsingKeyFrames>
                            <DoubleAnimation x:Name="PanelExpandTranslateStart" Duration="0:0:0.5" From="200" To="0" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)" Storyboard.TargetName="DirectionsFloatingPanel">
                                <DoubleAnimation.EasingFunction>
                                    <QuarticEase EasingMode="EaseInOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </VisualTransition>
                </VisualStateGroup.Transitions>

                <VisualState x:Name="PanelExpanded">
                    <Storyboard x:Name="ExpandedHeightStoryboard">
                        <ObjectAnimationUsingKeyFrames EnableDependentAnimation="True" Storyboard.TargetName="DirectionsFloatingPanel" Storyboard.TargetProperty="Height">
                            <DiscreteObjectKeyFrame x:Name="ExpandedHeightKeyFrame" KeyTime="0" Value="400"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
                <VisualState x:Name="PanelCollapsed">
                    <Storyboard x:Name="CollapsedHeightStoryboard">
                        <ObjectAnimationUsingKeyFrames EnableDependentAnimation="True" Duration="0:0:0" Storyboard.TargetName="DirectionsFloatingPanel" Storyboard.TargetProperty="Height">
                            <DiscreteObjectKeyFrame x:Name="CollapsedHeightKeyFrame" KeyTime="0" Value="100"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="GrabHandleStateGroup">
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="MouseOver">
                    <VisualState.Setters>
                        <Setter Target="GrabHandleHighlight.(UIElement.Visibility)" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ListView x:Name="TripResultList"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollMode="Auto"
                  ScrollViewer.IsHorizontalRailEnabled="True"
                  ScrollViewer.IsVerticalRailEnabled="False"
                  DataContext="{Binding ElementName=ThisTripResultContent, Path=DataContext}"
                  ItemsSource="{x:Bind ViewModel.TripResults, Mode=OneWay}"
                  SelectedItem="{x:Bind SelectedItem, Mode=TwoWay}"
                  ShowsScrollingPlaceholders="False"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <tripPlanStrip:TripPlanStrip StartingPlaceName="{Binding StartingPlaceName}"
                                                 EndingPlaceName="{Binding EndingPlaceName}"
                                                 StripItinerary="{Binding BackingItinerary}"
                                                 Margin="10 0 10 15" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <local:DigiTransitMap x:Name="SingleMap"
                              x:DeferLoadStrategy="Lazy"                                                       
                              Visibility="Collapsed" />

        <!--Height set at runtime in code-behind to 3/4 available vertical space.-->
        <Grid x:Name="DirectionsFloatingPanel"
              x:DeferLoadStrategy="Lazy"              
              Background="{ThemeResource HalfTransparentPageBackgroundBrush}"
              Height="400"              
              VerticalAlignment="Bottom"
              Visibility="Collapsed"
              Loaded="DirectionsFloatingPanel_Loaded">
            <Grid.RenderTransform>
                <CompositeTransform/>
            </Grid.RenderTransform>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid x:Name="GridGrabHeader"
                       Height="15"
                       HorizontalAlignment="Stretch"                        
                       Background="{ThemeResource SystemControlBackgroundAccentBrush}" 
                       Tapped="GridGrabHeader_Tapped">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Rectangle x:Name="GrabHandleHighlight" 
                           Grid.RowSpan="2" 
                           Fill="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                           Height="13"                           
                           Opacity=".5"
                           Visibility="Collapsed"/>
                <Rectangle Grid.Row="0" 
                           Fill="White"                           
                           Height="1"                           
                           Margin="0 4 0 0"
                           Width="20"/>
                <Rectangle Grid.Row="1" 
                           Fill="White"
                           Height="1"
                           Margin="0 0 0 4"
                           Width="20"/>
                <Interactivity:Interaction.Behaviors>
                    <Core:EventTriggerBehavior EventName="PointerEntered">
                        <Core:GoToStateAction StateName="MouseOver"/>
                    </Core:EventTriggerBehavior>
                    <Core:EventTriggerBehavior EventName="PointerExited">
                        <Core:GoToStateAction StateName="Normal"/>
                    </Core:EventTriggerBehavior>
                </Interactivity:Interaction.Behaviors>
            </Grid>

            <!--ItemsSource set in code-behind -->
            <ListView x:Name="DetailedTripList"                      
                      Grid.Row="1"
                      SelectionMode="None"
                      IsItemClickEnabled="True">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="apiModels:ApiLeg">
                        <TextBlock Text="{x:Bind From.Name}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

    </Grid>
</UserControl>