<UserControl
    x:Class="DigiTransit10.Controls.TripPlanStrip"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DigiTransit10.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:DigiTransit10.Models"
    mc:Ignorable="d"
    d:DesignHeight="80"
    d:DesignWidth="460">
    <ItemsControl x:Name="ItemsStrip" ItemsSource="{x:Bind TripLegs, Mode=OneWay}" >
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="local:TripLegViewModel">
                <Grid x:Name="LegContentRoot">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <local:TripPlanPoint StartTime="{x:Bind BackingLeg.StartTime.Value}"
                                         PlaceName="{x:Bind BackingLeg.From.Name}"
                                         Grid.Column="0"/>
                    <local:TripPlanTransitMethod TransitMode="{x:Bind BackingLeg.Mode.Value}"
                                                 ShortName="{x:Bind BackingLeg.Route.ShortName}"
                                                 Distance="{x:Bind BackingLeg.Distance}"
                                                 Grid.Column="0"
                                                 HorizontalAlignment="Center"
                                                 Grid.ColumnSpan="3"
                                                 Margin="10 0"/>
                    <local:TripPlanPoint StartTime="{x:Bind NextLeg.StartTime.Value}"
                                         Visibility="{x:Bind IsEnd, Converter={StaticResource BoolToVisibilityConverter}}"
                                         PlaceName="{x:Bind BackingLeg.To.Name}"
                                         Grid.Column="2"/>
                    
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
