<UserControl
    x:Class="DigiTransit10.Controls.TripPlanPoint"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DigiTransit10.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="TripPlanPointRootLayout" x:FieldModifier="Public">
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TimeBlock" 
                   Text="{x:Bind StartTime, Converter={StaticResource UnixTimeToLocalTimeConverter}}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"/>
        <Ellipse x:Name="LegCircle"                              
                 Grid.Row="1" 
                 Width="20" Height="20" 
                 HorizontalAlignment="Center"
                 Fill="{ThemeResource AppBarSeparatorForegroundThemeBrush}"/>
        <TextBlock x:Name="NameBlock"
                   Grid.Row="2"
                   Text="{x:Bind PlaceName}"
                   HorizontalAlignment="Center"/>

    </Grid>
</UserControl>
